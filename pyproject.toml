[project]
name = "juriscan"
version = "2.0.0"
description = "JuriScan — Análise forense de processos judiciais brasileiros. Skill para Claude Code."
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
